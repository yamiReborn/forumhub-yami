# Configura��o do servidor
server.port=3306
server.servlet.context-path=/api

# Configura��o do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/meuServidor
spring.datasource.username=usuario
spring.datasource.password=senha
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.jpa.hibernate.ddl-auto=update
spring.datasource.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configura��o do JPA (Java Persistence API)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configura��o de seguran�a (se necess�rio)
spring.security.user.name=admin
spring.security.user.password=admin123

# Log level
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# Configura��o de cache (opcional)
spring.cache.type=simplespring.application.name=demo
